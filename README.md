# Flask CSV Reader

A simple Flask web app to upload and display CSV files.

## Usage

1. Install dependencies:
   ```
   pip install -r requirements.txt
   ```
2. Run the app:
   ```
   python app.py
   ```
3. Open your browser at http://127.0.0.1:5000
4. Upload a CSV file to view its contents.
